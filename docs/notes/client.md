# Client Notes

- The `get_graph_layout`, `graph_neighbors`, `get_vault_settings`, and `update_vault_settings` client wrappers are intentionally retained ahead of `COMP-GTK-GRAPH-006` and `COMP-GTK-SETTINGS-007`. They are currently unused in production code but still define the daemon contract that those slices will build on.
